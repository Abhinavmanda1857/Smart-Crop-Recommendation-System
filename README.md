# Smart Crop Recommendation 🌾🌱

This project is a web application that uses a Random Forest model to recommend the best crop based on soil and climate parameters and predict expected yield.

## 🚀 Features
- Predicts the most suitable crop using Random Forest.
- Provides estimated yield in tons/hectare.
- Web interface with clean HTML/CSS/JS frontend.
- Flask-based backend API.
- Trained model and scaler integrated.

## 🏗 Tech Stack
- **Frontend**: HTML, CSS, JavaScript
- **Backend**: Python, Flask
- **ML Model**: Random Forest
- **Data**: Enhanced crop dataset (Excel)

## 📦 Setup Instructions

### Prerequisites
- Python 3.x
- Install dependencies:
  ```bash
  pip install -r requirements.txt
